# Django Settings
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/didactia_db

# Supabase Configuration (if using Supabase)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-supabase-anon-key
SUPABASE_SERVICE_KEY=your-supabase-service-key

# AWS S3 Configuration (if using S3)
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_STORAGE_BUCKET_NAME=your-bucket-name
AWS_S3_REGION_NAME=us-east-1

# AI/Gemini Configuration
GEMINI_API_KEY=your-gemini-api-key-here
HUGGINGFACE_API_TOKEN=your-huggingface-token

# Redis Configuration (for Celery)
REDIS_URL=redis://localhost:6379/0

# Email Configuration (REQUIRED for password reset functionality)
# The system will automatically choose SMTP if credentials are provided,
# otherwise it will use console backend (emails appear in terminal)

# Gmail Configuration:
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_USE_SSL=False
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password  # Use App Password, not regular password

# Outlook/Hotmail Configuration (alternative):
# EMAIL_HOST=smtp-mail.outlook.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_USE_SSL=False
# EMAIL_HOST_USER=your-email@outlook.com
# EMAIL_HOST_PASSWORD=your-password

# Email sender information
DEFAULT_FROM_EMAIL=DidactAI <noreply@yourdomain.com>
SERVER_EMAIL=DidactAI <server@yourdomain.com>
SERVER_NAME=localhost:8000

# Sentry Configuration (for monitoring)
SENTRY_DSN=your-sentry-dsn

# File Upload Settings
MAX_UPLOAD_SIZE=50000000  # 50MB in bytes
ALLOWED_FILE_EXTENSIONS=pdf,docx,pptx,png,jpg,jpeg

# AI Generation Settings
DEFAULT_LANGUAGE=en
SUPPORTED_LANGUAGES=en,fr,es,de